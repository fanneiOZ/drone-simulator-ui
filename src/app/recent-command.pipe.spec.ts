import { RecentCommandPipe } from './recent-command.pipe';

describe('RecentCommandPipe', () => {
  it('create an instance', () => {
    const pipe = new RecentCommandPipe();
    expect(pipe).toBeTruthy();
  });
});
