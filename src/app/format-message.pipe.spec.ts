import { FormatMessagePipe } from './format-message.pipe';

describe('FormatMessagePipe', () => {
  it('create an instance', () => {
    const pipe = new FormatMessagePipe();
    expect(pipe).toBeTruthy();
  });
});
