import { FormatPositionPipe } from './format-position.pipe';

describe('FormatPositionPipe', () => {
  it('create an instance', () => {
    const pipe = new FormatPositionPipe();
    expect(pipe).toBeTruthy();
  });
});
